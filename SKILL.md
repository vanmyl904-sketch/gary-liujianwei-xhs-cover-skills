---
name: gary-liujianwei-xhs-cover
description: Generate finished Chinese social-media cover images from a user-selected visual style, one main image, an optional set of up to three supporting images, a main title, and up to three AI-suggested subtitles. Use when the user asks to make, design, generate, redraw, or produce a 小红书封面, 社交媒体封面, 笔记首图, 图文封面, 视频封面, or wants a cover matching one of this skill's bundled reference styles.
---

# Gary刘坚伟-小红书封面设计-skills

直接生成最终封面图片，不要只输出提示词。

## 工作流

1. 读取用户当前消息中已提供的文案、标题、图片和风格偏好，不重复询问。
2. 如果用户只有一段文案没有标题，先从文案中提炼 3 个不同角度的主标题候选（每条 4–14 个汉字），让用户选择 1 个或自行改写。标题要具体、有信息差，避免空泛鸡汤。
3. 如果用户还没有选风格，读取 [styles.md](references/styles.md)，用 9 张参考图及名称让用户选一个编号。选择消息必须同时展示每种风格的缩略图预览（可用 Markdown 图片标签或界面支持的图片卡片），不能只列编号和文字。不要增加参考表之外的风格。
4. 收集以下最少信息；缺少时一次性列出，不要逐项追问：
   - 1 张主图，必需。主图是封面的核心人物、作品或场景。
   - 0–3 张附图，可选。附图是产品、案例、课程截图、Logo 或其他需要进入封面的素材。
   - 1 个主标题，必需，建议 4–14 个汉字。
   - 如果主图包含真人且用户强调个人品牌，默认启用“真人严格还原”：可额外接收 1–3 张清晰的人脸/半身参考图，用于锁定身份；没有额外参考图时，不要求用户重复上传。
5. 选择锁定规则：收到图片、标题或“直接生成”等指令后，仍然必须检查以下选择是否已由用户明确给出。任何一项未选，必须先一次性展示全部未选项并等待用户回复；不得擅自补全、默认跳过或直接生成。用户明确回复“交给模型决定”才可跳过对应项。
   - 比例：`1 9:16 竖版视频/抖音封面  2 3:4 小红书图文封面  3 4:3 通用图文封面  4 16:9 横版视频/公众号封面  5 1:1 方形社媒封面  6 交给模型决定`。
   - 有人物时，让用户选择一个“人物表现”，把表情和动作合并成一次选择：`1 捂嘴惊讶  2 张嘴震惊  3 开心大笑  4 兴奋雀跃  5 自信得意  6 托腮思考  7 推荐种草感  8 认真讲解  9 指向标题  10 沉思判断  11 自然微笑  12 交给模型决定`。用户也可以直接描述想要的表情或动作；没有人物时跳过此项。
6. 视觉偏好同样属于选择锁定项；字体与背景色调必须一次性展示，不能拆成多轮：
   - 字体：超粗黑体、柔和圆体、手写涂鸦体、极简无衬线、复古宋体、交给模型决定。
   - 背景：浅色系、深色系、暖色调、冷色调、高饱和撞色、交给模型决定。
7. 根据主标题生成 3 条不同角度的短副标题，每条 4–10 个汉字。避免三条表达同一件事。副标题选择同样必须锁定：`可回复 1/2/3（可多选）、全选、都不要或交给模型决定。` 用户也可以改写候选。
8. 只有风格、比例、人物表现（如适用）、字体、背景和副标题都已经明确选择，或用户对未选项明确说“交给模型决定”后，才读取所选风格在 [styles.md](references/styles.md) 中的完整规范并开始生成。
9. 对文案做一次“语义视觉化”分析：提取其中最重要的 1–4 个具体名词、工具、场景或隐喻，自动设计为配图元素，并融入所选构图。例如文案提到苹果、软件网页、小龙虾、相机、时钟或渠道，可以生成相应的物件、插画、卡片或抽象界面。配图必须服务于主题，不得喧宾夺主。
10. 调用可用的图像生成工具生成最终图片。把所选风格图作为视觉语言参考，把用户主图和附图作为内容参考，同时把语义视觉化元素作为内容参考。

## 图片指代

- 风格参考图：只借鉴字体、配色、版式、描边、拼贴和信息层级；不得复刻参考图中的人物、场景、原文字或品牌。
- 用户图 1：主图。必须保持可识别主体，视觉优先级最高。
- 用户图 2–4：附图。按所选风格放入小卡片、拼贴区或结果证明区，不得反客为主。
- 没有附图时，不要虚构截图、Logo、数据证明或额外人物照片。
- 人物表现：只影响主图人物的表情、视线和手势，不改变人物身份；必须严格服从用户最后选择，不能被原图默认表情覆盖。例如用户选择“沉思判断”时，应呈现托腮、眉眼收敛、认真思考的状态，即使原图在微笑；没有人物时不强行添加人物。
- 真人严格还原（默认）：人物脸型、五官比例、眼睛、鼻子、嘴型、发际线、肤色、年龄感、眼镜和标志性配饰必须优先保持；禁止擅自美颜、换脸、年轻化、改变发型或生成“相似但不是本人”的新脸。身份保持与用户选择的表情/动作同时成立：只改变表情肌肉、视线和姿态，不改变身份特征。优先使用支持身份保持/图像编辑的生成模式，把原图人物作为前景主体，只生成背景、文字和配图元素；不要让模型重新设计人物脸部。
- 语义视觉化元素：优先来自用户文案中的名词和隐喻；可生成物件、场景、插画、装饰性界面或图标。没有文案依据的元素不添加；除非用户提供或明确要求，不伪造品牌 Logo、真实网页截图、统计数据、客户评价或平台 UI。

## 生成要求

- 严格使用用户选定的比例：9:16、3:4、4:3、16:9 或 1:1。比例尚未选择时不得生成；仅当用户明确选择“交给模型决定”时，才根据发布场景决定，默认优先 3:4。
- 主标题必须原样、完整、清晰地写入画面，不要改写。
- 只写入用户选中的副标题；用户选择“都不要”时，禁止擅自添加宣传文案。
- 中文必须可辨认，不出现乱码、伪文字、拼音替代或无意义英文。
- 保持主标题第一、主图第二、副标题第三、附图第四的视觉层级。
- 不生成二维码、平台 UI、关注按钮、虚假点赞评论、价格或未经用户提供的品牌标识。
- 保持手机信息流缩略图下仍能一眼读懂；避免细字、低对比和元素贴边。
- 生成失败或文字错误时，保留原选择直接重试，不重新询问全部信息。
- 人物不像本人时，不接受“差不多”；优先降低人物重绘程度、保留原图脸部并重试。连续两次仍不像时，提示用户补充 1–3 张正面/侧面清晰参考图，再继续生成。

## 提示词组装顺序

向图像生成工具描述时按以下顺序组织：

1. 输出规格与用途。
2. 所选风格的版式、字体、配色与装饰规范。
3. 主图位置、大小和主体保持要求；若有人物，先写真人严格还原约束，再写构图和动作。
4. 主标题原文和分行逻辑。
5. 用户选中的副标题原文及位置。
6. 附图的编号、用途与位置。
7. 从文案提取的语义视觉化元素、它们的视觉形式、数量和位置。
8. 安全区、可读性和禁止项。

不要把 9 种风格混在一起。一次只应用用户选择的一种风格。
